{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug philo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/philo",
            "args": ["2", "120", "60", "60"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": false,  // Disable external console
            "MIMode": "gdb"
        }
    ]
}